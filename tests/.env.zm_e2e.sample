# ZoneMinder E2E test configuration
#
# One-time setup:
#   sudo pip install pytest --break-system-packages
#   cp tests/.env.zm_e2e.sample .env.zm_e2e
#   # edit .env.zm_e2e with your ZM server details
#
# Readonly tests:
#   sudo -u www-data python -m pytest tests/test_zm_e2e/ -v -p no:cacheprovider
#
# Include write tests (event notes, stop/start/restart, DB tagging):
#   sudo -u www-data ZM_E2E_WRITE=1 python -m pytest tests/test_zm_e2e/ -v -p no:cacheprovider
#
# .env.zm_e2e is gitignored — your credentials stay local.

ZM_API_URL=https://your-zm-server/zm/api
ZM_USER=admin
ZM_PASSWORD=yourpassword
ZM_VERIFY_SSL=false

# Override if your zm.conf is in a non-standard location:
#PYZM_CONFPATH=/etc/zm
